import { Sora, Work_Sans, Inter } from "next/font/google";

export const sora = Sora({
  subsets: ["latin"],
  display: "swap",
  adjustFontFallback: false,
});
export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  adjustFontFallback: false,
});

export const work_sans = Work_Sans({
  subsets: ["latin"],
  display: "swap",
  adjustFontFallback: false,
});
